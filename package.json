{
  "name": "asteroids-tech-challenge",
  "version": "1.0.0",
  "description": "Full-stack application for a tech challenge about asteroids",
  "scripts": {
    "dev": "npm run dev:client & npm run dev:server",
    "dev:client": "cd asteroids-client; npm i; npm run dev",
    "dev:server": "cd asteroids-server; npm i; npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Antonio Campos",
  "license": "ISC",
  "dependencies": {
  }
}
